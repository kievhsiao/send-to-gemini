{
  "name": "send-to-gemini",
  "version": "1.1",
  "description": "Send selected text to Google Gemini via context menu, or clip page sections as Markdown",
  "scripts": {
    "build": "tsc && cmd /c npx esbuild src/clipper_content.ts --bundle --outfile=dist/clipper_content.js --platform=browser --target=chrome120 --external:chrome"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/chrome": "^0.0.240",
    "esbuild": "^0.25.1",
    "turndown": "^7.2.0",
    "@types/turndown": "^5.0.5"
  },
  "dependencies": {
    "turndown": "^7.2.0"
  }
}